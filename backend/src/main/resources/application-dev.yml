spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/pushpal
    username: pushpal
    password: password
  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true

app:
  jwt:
    secret: ${JWT_SECRET:pushpal-development-secret-at-least-256-bits-long}
