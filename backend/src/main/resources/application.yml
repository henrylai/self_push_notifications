server:
  port: ${PORT:8080}

spring:
  datasource:
    url: ${DATABASE_URL:jdbc:postgresql://localhost:5432/pushpal}
    username: ${DB_USERNAME:pushpal}
    password: ${DB_PASSWORD:password}
    hikari:
      maximum-pool-size: 10
  mail:
    host: ${SMTP_HOST:}
    port: ${SMTP_PORT:587}
    username: ${SMTP_USER:${SMTP_USERNAME:}}
    password: ${SMTP_PASS:${SMTP_PASSWORD:}}
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
  jpa:
    hibernate:
      ddl-auto: validate
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
    show-sql: false
  flyway:
    enabled: true
    locations: classpath:db/migration
  jackson:
    default-property-inclusion: non_null

app:
  jwt:
    secret: ${JWT_SECRET:dev-secret-change-in-production-at-least-256-bits-long!!}
    expiration-days: 7
  vapid:
    public-key: ${VAPID_PUBLIC_KEY:}
    private-key: ${VAPID_PRIVATE_KEY:}
    subject: ${VAPID_SUBJECT:mailto:pushpal@example.com}
  google:
    client-id: ${GOOGLE_CLIENT_ID:}
    client-secret: ${GOOGLE_CLIENT_SECRET:}
    token-uri: https://oauth2.googleapis.com/token
    userinfo-uri: https://www.googleapis.com/oauth2/v2/userinfo
  magic-link:
    expiration-minutes: 43200
    base-url: ${MAGIC_LINK_BASE_URL:http://localhost:3000}
  mail:
    from: ${MAIL_FROM:${SMTP_USER:PushPal <noreply@pushpal.app>}}
  api-base-url: ${API_BASE_URL:}
  scheduler:
    enabled: true
    interval-ms: 30000

management:
  endpoints:
    web:
      exposure:
        include: health,info
  endpoint:
    health:
      show-details: when_authorized
  health:
    mail:
      enabled: false
